import { cpu6510 } from "../cpu6510";

/**
 * ## ASL -> accumulator
 * @description Shift left one bit (memory or accumulator).
 * @private
 */
export function ASL(this: cpu6510) { throw "Error: Not implemented!" }
/**
 * ## ASL -> zeropage
 * @description Shift left one bit (memory or accumulator).
 * @private
 */
export function ASL_Z(this: cpu6510) { throw "Error: Not implemented!" }
/**
 * ## ASL -> zeropage, x
 * @description Shift left one bit (memory or accumulator).
 * @private
 */
export function ASL_ZX(this: cpu6510) { throw "Error: Not implemented!" }
/**
 * ## ASL -> absolute
 * @description Shift left one bit (memory or accumulator).
 * @private
 */
export function ASL_A(this: cpu6510) { throw "Error: Not implemented!" }
/**
 * ## ASL -> absolute, x
 * @description Shift left one bit (memory or accumulator).
 * @private
 */
export function ASL_AX(this: cpu6510) { throw "Error: Not implemented!" }